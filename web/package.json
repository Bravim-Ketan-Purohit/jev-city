{
  "name": "@jev-city/web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "preview": "vite preview",
    "sim:check": "tsx scripts/sim-check.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  },
  "dependencies": {
    "@jev-city/shared": "workspace:*"
  }
}
